<div class="container">
  <div class="row">
    <%# <h1><%= "#{@user.username.capitalize}'s Profile"%></h1>
    <h3>Watchlist</h3>
  </div>

  <div class="row">
    <div class="watchlist-cards">
      <% @movies.each do |movie| %>
        <div class="watchlist-card">
          <div class="card-image">
            <%= image_tag(movie.poster_url, alt: "#{movie.title} cover art", width: 165, class: "watchlist-poster") %>
          </div>
          <div class="card-content">
            <p> <%= "#{movie.runtime.to_s} mins / #{movie.year.to_s}" %> </p>
            <%# <h4><i class="fa-solid fa-angle-down"></i></h4> %>

            <h4> <%= movie.title %> </h4>

            <%= link_to ('<i class="fa fa-trash" aria-hidden="true"></i>').html_safe,
               # Must find the movie bookmark id - do this by gettind this into the user conteoller and finding one with the movie_id that matches the
                { controller: "movie_bookmarks", action: "destroy", id: @movie_bookmarks.find_by(movie_id: movie.id).id },
                  data: { confirm: 'Are you sure?' },
                  method: :delete %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>



            <%# <%= link_to "Remove",
               # Must find the movie bookmark id - do this by gettind this into the user conteoller and finding one with the movie_id that matches the
                { controller: "movie_bookmarks", action: "destroy", id: @movie_bookmarks.find_by(movie_id: movie.id).id },
                  data: { confirm: 'Are you sure?' },
                  method: :delete %>
